Для сборки проекта потребуется:
1. Apache Ant
2. Closure Compiler
3. Системная переменная с именем CLOSURE_COMPILER, значение которой — это путь к папке с файлом compiler.jar (компилятором)

Собрать проект можно в двух типах:
1. Продакш код (с сжатием js кода)
2. код для отладки и тестирования (без сжатия js кода)

Процесс сборки:
Перейти в папку с проектом (где лежит build-resume.xml)
В коммандной строке выполнить сдующее:
для продакш сборки:	ant -f build-resume.xml
для дебаг сборки:	ant -f build-resume.xml debug

Если нет желания производить сборку, то можно сразу воспользоваться результатами в папке build-resume

